<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.frankun.dao.UserMapper">

    <sql id="userColumns">
        a.guid = #{guid}
        a.create_time = #{createTime}
        a.archived =#{archived}
        a.email = #{email}
        a.password = #{password}
        a.phone = #{phone}
        a.username = #{username}
        a.default_user = #{defaultUser}
        a.last_login_time = #{lastLoginTime}
    </sql>

    <select id="findByUsername" parameterType="String" resultType="com.frankun.domain.user.User">
        SELECT
            <include refid="userColumns"/>
        FROM user_
        WHERE username = #{username}
    </select>

    <select id="findByUsername" parameterType="String" resultType="java.util.List">
        select * from user_ where 1=1
        <if test="loginName != null and loginName != '' "> and login_name like concat('%',#{loginName},'%')</if>
    </select>
    
</mapper>